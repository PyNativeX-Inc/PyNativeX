# Hello World App

Une application simple créée avec PyNativeX pour afficher "Hello World".

## Lancer l'application

Pour inspecter l'application avec la CLI PyNativeX :

```bash
PYTHONPATH=apps/hello_world pynativex inspect \
  --entry hello_world.main:app \
  --pretty
```
